class ChatMessage {
    constructor(text, author, timestamp) {
        this.id = 999;
        this.text = text;
        this.author = author;
        this.timestamp = timestamp;
    }
}

export default ChatMessage;